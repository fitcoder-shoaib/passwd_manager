# passwd_manager
cs python passwd manager.
download all the files in a single folder.
then run it by using command "python3 gui.py"


this program is a self-coded basic python code of a password manager
made as a cs project. btech first year
